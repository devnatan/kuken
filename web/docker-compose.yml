services:
  webui:
    image: ghcr.io/kuken-project/webui:latest
    container_name: kuken-webui
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - VITE_KUKEN_API=${KUKEN_API}
    volumes:
      - ./logs:/var/log/nginx
    restart: unless-stopped
